---
layout: ../layouts/BaseLayout.astro
import { getRepoStats } from '~/lib/github';

const repoName = 'tiagovilasboas/awesome-front-end-architecture';
const ghStats = await getRepoStats(repoName);

const featuredPatterns = [
  {
    slug: 'atomic-design',
    name: 'Atomic Design',
    tagline: 'A methodology for creating design systems.',
    icon: 'atom',
  },
  {
    slug: 'islands-architecture',
    name: 'Islands',
    tagline: 'Build faster websites with less JavaScript.',
    icon: 'sailboat',
  },
  {
    slug: 'state-machines',
    name: 'State Machines',
    tagline: 'Design complex UIs with predictable state.',
    icon: 'workflow',
  },
];
---

import { Icon } from 'lucide-astro';
import PatternsGrid from '~/components/PatternsGrid.astro';
import { Button } from '~/lib/ui/button';

<section class="flex flex-col items-center justify-center text-center py-20">
  <h1 class="text-4xl font-extrabold tracking-tighter sm:text-5xl md:text-6xl">
    Front-End Architecture that Scales (and stays sane)
  </h1>
  <p class="mx-auto mt-6 max-w-2xl text-lg text-muted-foreground">
    A curated collection of battle-tested patterns and practices for building modern, maintainable, and scalable front-end applications.
  </p>
  <div class="mt-8 flex flex-wrap justify-center gap-4">
    <a href={`https://github.com/${repoName}`} target="_blank" class="inline-flex items-center justify-center rounded-md bg-primary px-4 py-2 text-sm font-medium text-primary-foreground shadow transition-colors hover:bg-primary/90 focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring">
      <Icon name="star" class="mr-2 h-4 w-4" />
      Star
      {ghStats.stars > 0 && <span class="ml-2 rounded-md bg-primary-foreground/20 px-2 py-0.5 text-xs">{ghStats.stars}</span>}
    </a>
    <a href={`https://github.com/${repoName}/fork`} target="_blank" class="inline-flex items-center justify-center rounded-md border border-input bg-background px-4 py-2 text-sm font-medium shadow-sm transition-colors hover:bg-accent hover:text-accent-foreground focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring">
      <Icon name="git-fork" class="mr-2 h-4 w-4" />
      Fork
      {ghStats.forks > 0 && <span class="ml-2 rounded-md bg-muted px-2 py-0.5 text-xs">{ghStats.forks}</span>}
    </a>
    <a href={`https://github.com/${repoName}/issues`} target="_blank" class="inline-flex items-center justify-center rounded-md border border-input bg-background px-4 py-2 text-sm font-medium shadow-sm transition-colors hover:bg-accent hover:text-accent-foreground focus-visible:outline-none focus-visible:ring-1 focus-visible:ring-ring">
      <Icon name="alert-circle" class="mr-2 h-4 w-4" />
      Issues
      {ghStats.openIssues > 0 && <span class="ml-2 rounded-md bg-muted px-2 py-0.5 text-xs">{ghStats.openIssues}</span>}
    </a>
  </div>
</section>

<div class="my-12">
  <h2 class="mb-8 text-center text-3xl font-bold tracking-tighter">Featured Patterns</h2>
  <PatternsGrid patterns={featuredPatterns} />
</div> 