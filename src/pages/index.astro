---
import BaseLayout from '../components/layouts/BaseLayout.astro';
import PatternsGrid from '../components/sections/PatternsGrid.astro';
import CallToAction from '../components/sections/CallToAction.astro';
import AboutAuthor from '../components/sections/AboutAuthor.astro';
import { getAllPatterns } from '../lib/patterns';

const allPatterns = await getAllPatterns();
---
<BaseLayout>
  <header class="page-header">
    <h1 class="title">O Guia (Realista) para Arquitetura Front-end</h1>
    <p class="subtitle">
      Uma coleção de padrões e práticas, de dev pra dev, sem papas na língua. O objetivo é te ajudar a fazer as perguntas certas e tomar decisões melhores no seu próximo projeto.
    </p>
  </header>

  <div class="patterns-list">
    <h2 class="list-title">Padrões de Arquitetura & Metodologias</h2>
    <PatternsGrid patterns={allPatterns} />
  </div>

  <CallToAction />
  
  <AboutAuthor />
</BaseLayout>

<style>
  .page-header {
    text-align: center;
    margin-bottom: 4rem;
  }
  .title {
    font-size: 2.25rem;
    font-weight: 800;
    line-height: 1.2;
    margin-bottom: 1rem;
  }
  .subtitle {
    font-size: 1.125rem;
    color: var(--muted-foreground);
  }
  .list-title {
    font-size: 1rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--muted-foreground);
    border-bottom: 1px solid var(--border);
    padding-bottom: 0.5rem;
    margin-bottom: 2rem;
  }
  .patterns-list {
    margin-bottom: 4rem;
  }
</style> 